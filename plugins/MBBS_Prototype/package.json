{
  "name": "mbb",
  "version": "1.0.0",
  "description": "A plugin description",
  "license": "MIT",
  "author": "Ivan Xu",
  "main": "dist/MBBS_Prototype.js",
  "scripts": {
    "build": "rollup -c",
    "lint": "pnpm run lint:package && pnpm run lint:js",
    "lint:js": "eslint --fix --cache src --ext .js",
    "lint:package": "prettier --write package.json --plugin=prettier-plugin-package",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "rollup": "^2.26.5"
  }
}
