// this is an example plugin

Scene_Map = class extends Scene_Map {
  start(...args) {
    super.start(args);
  }

  update(...args) {
    super.update(args);
  }

  stop(...args) {
    super.stop(args);
  }
};
